# Audit Findings – Epic 07: Ownership Splits

**Datum auditu:** 2026-06-28  
**Auditor:** Claude Code (read-only, bez úprav kódu)  
**Základní dokument:** PRD v2 §8.5 (Ownership and Contributor Records), Requirement Index (`docs/audit/requirement-index.md`)  
**Pokryté ID:** R-8.5-01 až R-8.5-07, AC-05  
**Zkoumané soubory:**
- `prisma/schema.prisma` (ř. 492–553)
- `src/app/api/projects/[id]/splits/route.ts`
- `src/app/api/projects/[id]/splits/[splitId]/route.ts`
- `src/app/api/projects/[id]/splits/[splitId]/contributors/route.ts`
- `src/app/api/projects/[id]/splits/[splitId]/contributors/[contributorId]/route.ts`
- `src/app/api/projects/[id]/splits/[splitId]/submit/route.ts`
- `src/app/api/splits/confirmations/[confirmationId]/route.ts`
- `src/app/api/splits/confirmations/[confirmationId]/confirm/route.ts`
- `src/app/api/splits/confirmations/[confirmationId]/reject/route.ts`
- `src/app/projects/[id]/splits/page.tsx`
- `src/app/projects/[id]/splits/[splitId]/page.tsx`
- `src/app/splits/confirmations/[confirmationId]/page.tsx`
- `src/lib/email.ts` (ř. 130–160)

---

| ID | Požadavek | Status | Důkaz (soubor:řádek) | Úroveň | Mezera / poznámka | Issue# |
|----|-----------|--------|----------------------|--------|-------------------|--------|
| R-8.5-01 | Owner vytváří draft splitu | ✅ | `splits/route.ts:74–126` – POST autorizace `project.ownerId !== user.id` vrátí 403; vytvoří záznam se `status: "draft"`; `splits/page.tsx:162–170` – tlačítko „New Draft Split" volá POST | čteno | Vytvoření draftu owner-only, plně implementováno v API i UI. | — |
| R-8.5-02 | V prvním customer-ready release jsou splity pouze na úrovni projektu (ne track-level) | ✅ | `prisma/schema.prisma:511–528` – `SplitRecord` má pouze `projectId`, žádné `trackId` ani `fileId`; `splits/route.ts:14` – route parametr je `[id]` (projectId); žádná track-level asociace v celé splits API vrstvě | čteno | Pouze project-level split. Žádný track-level mechanismus neexistuje. | — |
| R-8.5-03 | Celkový split musí dávat 100 % před odesláním | ✅ | `splits/[splitId]/submit/route.ts:57–70` – `totalCents` počítán jako `Math.round(percentage * 100)`; podmínka `totalCents !== 10000` vrátí 422 s detailem součtu; celočíselná aritmetika zabraňuje float chybám | čteno | Validace 100% v transakční bráně submit endpointu; float-safe implementace. | — |
| R-8.5-04 | Přispěvatelé s nenulovým podílem musí split potvrdit | ✅ | `splits/[splitId]/submit/route.ts:45–53` – filtr `nonZeroContributors` (percentage > 0); `tx.splitConfirmation.createMany` vytváří záznamy pouze pro tyto přispěvatele (ř. 83–87); `confirmations/[id]/confirm/route.ts` + `reject/route.ts` implementují PUT pro odpověď | čteno | Pouze přispěvatelé s nenulovým podílem obdrží konfirmační záznam. Logika splněna. | — |
| R-8.5-05 | Potvrzené splity nelze editovat přímo (in place) | ✅ | `splits/[splitId]/contributors/route.ts:33–38` – guard `split.status !== "draft"` vrátí 409 pro add; `contributors/[contributorId]/route.ts:22–23` – stejný guard pro PUT/DELETE; `splits/[splitId]/route.ts:99–104` – DELETE blokován pokud `status !== "draft"` | čteno | Všechny mutační endpointy (add, update, delete contributor; delete split) jsou blokovány pro ne-draft stavy. UI také skrývá formulář a tlačítka pro ne-draft stav (page.tsx:205, 345, 374, 387). | — |
| R-8.5-06 | Změny splitu vyžadují novou revizi, která nahrazuje předchozí | ✅ | `splits/route.ts:100–123` – POST vyhledá aktuální confirmed split (`status: "confirmed", supersededById: null`) a propojí nový draft přes `supersedes: { connect }`; `confirmations/[id]/confirm/route.ts:104–118` – po plném potvrzení transakce nastaví předchozí confirmed split na `status: "superseded", supersededById: split.id`; `prisma/schema.prisma:516,523–524` – self-referenční `supersedes`/`supersededBy` relace s `@unique` na `supersededById` | čteno | Revision chain je atomicky udržována. UI zobrazuje „Revision history" panel s linky na předchozí/následní revision (page.tsx:238–283). | — |
| R-8.5-07 | Systém je záznamy o přispěvatelích a potvrzením – nikoli ingestion royalties ani právním vymáháním | ✅ | `prisma/schema.prisma:492–553` – modely neobsahují žádné royalty, payout ani legal pole; `SplitRecord` a `SplitContributor` uchovávají pouze `percentage`, `role`, `status`, `confirmation`; žádné napojení na Stripe Connect ani `PayoutRecord` v splits modelech | čteno | Systém striktně omezen na záznamy a potvrzení. Žádný finanční mechanismus v splits vrstvě. | — |
| AC-05 | Vytvoření a potvrzení ownership splitu musí fungovat end-to-end | 🟡 | **API:** Plný happy-path chain existuje: POST draft → POST contributor → PUT contributor → DELETE contributor → POST submit → GET confirmation → PUT confirm/reject (vše implementováno). **UI gap:** `src/app/projects/[id]/splits/[splitId]/page.tsx` neobsahuje tlačítko „Submit for Confirmation" ani volání na `/submit` endpoint (grep potvrdil: žádný výskyt `/submit` ani `handleSubmit` pro split). Uživatel nemůže odeslat draft ke konfirmaci bez přímého API volání. **Notifikace:** `submit/route.ts:111` – TODO komentář: „Send notification to each contributor (notification service not yet implemented)"; přispěvatelé nejsou informováni e-mailem o nutnosti potvrdit split. Zpětná notifikace ownera (confirm/reject) implementována v `email.ts:138–160`. | čteno | ⚠️ PRD §8.5 vyžaduje, aby přispěvatelé mohli potvrdit split — to předpokládá, že se o něm dozvědí. Submit notifikace chybí (TODO v kódu). UI postrádá tlačítko pro odeslání draftu ke konfirmaci, čímž je end-to-end tok přerušen pro běžného uživatele. | — |
| — | UI: tlačítko „Submit for Confirmation" v editoru draftu | ❌ | `src/app/projects/[id]/splits/[splitId]/page.tsx` – prohledáno 449 řádků; žádné volání na `${apiBase}/submit`; `submittedAt` je součástí interface (ř. 36, 44) ale nikde není tlačítko, které by POST na `/submit` odesílalo | čteno | ⚠️ API `POST /api/projects/[id]/splits/[splitId]/submit` existuje a je plně implementováno (submit/route.ts), ale UI vstupní bod zcela chybí. Toto je bloker pro end-to-end tok AC-05. Nesouvisí přímo s R-8.5-* ID (nejde o samostatný požadavek), ale blokuje splnění AC-05. | — |
| — | Notifikace přispěvatelů po odeslání draftu ke konfirmaci | ❌ | `splits/[splitId]/submit/route.ts:111` – explicitní TODO: „Send notification to each contributor (notification service not yet implemented)"; `src/lib/email.ts` – neobsahuje funkci pro notifikaci přispěvatele o nutnosti potvrdit split | čteno | ⚠️ PRD §8.5 implicitně vyžaduje, že přispěvatelé s nenulovým podílem „must confirm" — bez notifikace se nemohou dozvědět, že mají otevřenou konfirmaci. Zpětná notifikace ownera (confirm/reject) implementována v `sendConfirmationResponseEmail`. Forward notifikace přispěvateli chybí. | — |
| — | `ConfirmationStatus.expired` — mechanismus expirace | ⚠️ | `prisma/schema.prisma:504–509` – enum `ConfirmationStatus` obsahuje hodnotu `expired`; `SplitConfirmation` model (ř. 545–553) **neobsahuje** pole `expiresAt` ani `expiredAt`; žádný cron/background job pro přechod `pending → expired` nalezen v celém projects adresáři | čteno | ⚠️ `expired` stav je deklarován v enum a zobrazován v UI (splits/page.tsx:19, [splitId]/page.tsx:65, confirmations/page.tsx:207), ale neexistuje žádný mechanismus, který by ho nastavil. PRD §8.5 expirace explicitně neuvádí, ale stav v enum a UI badge ji implikuje. Bez expirace zůstanou staré pending konfirmace ve stavu `pending` navždy. | — |
